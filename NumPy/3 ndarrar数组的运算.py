#                    本节课主要内容如下简单描述
# 1.
# 跟标量之间的运算(除法的话 是对adarray数组中每一个元素都除法)

# 2.
# 可以数组中各元素的绝对值 平方根 平方 取对数 向下取整 向上取整
#                  四舍五入
#                  将数组个元素的小数和整数部分以两个独立数组形式返回
#                  计算各元素的符号 各元素的整数值等等 好多方法
import  numpy as np
a = np.arange(24).reshape((2,3,4))
a = np.sqrt(a)
print(np.modf(a))

# 3.
# 还有二元函数 可以进行两个数组 元素级的模运算
#              可以进行两个数组 算数比较级的运算 返回一个True False数组

# 这块也得注意看数组在进行计算过后是否会发生变化
# numpy设计理念是希望我们把数组当成元素对待